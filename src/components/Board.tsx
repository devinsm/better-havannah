import React from 'react';

const Board: React.ComponentType = () => {
  return (
    <svg aria-labelledby="game-board-title" role="group" viewBox="0 0 100 100">
      <title id="game-board-title">Game Board</title>
    </svg>
  );
};

export default Board;
